双指针法（快慢指针法）在数组和链表的操作中是非常常见的，很多考察数组、链表、字符串等操作的面试题，都使用双指针法。
快指针指向新数组需要的元素（也就是用来覆盖的元素）
慢指针指向需要被覆盖的元素（也就是需要删除的元素）
本质上就是一个重建数组的过程，快指针指向的是新数组需要的元素，慢指针指向的是新数组中元素需要插入的位置。
因为是在原数组上操作的，所以有些难理解
需要注意的是return时return slow即可，因为此时下标的数值就是新数组的size.
