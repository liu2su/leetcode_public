3sum  使用哈希法并不合适，因为需要去重，很难 bug free
本题还是需要双指针法来解决
在使用双指针法之前，要将整个数组进行排序.
有三点需要注意：
1.while 结束时机，当left等于right时，while循环结束
2.数组去重，因为数组经过排序后，a如果遇到重复的值，那么一定会与重复的bc组成重复的数组，思考数组{-1，-1，2},a应该与前一个元素作比较。
3.在获得数组后，left++，right--
