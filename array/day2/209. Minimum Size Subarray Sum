 209. Minimum Size Subarray Sum

    暴力解法的时间复杂度为O（n^2）
    slide windows:
    在暴力解法中，是一个for循环滑动窗口的起始位置，一个for循环为滑动窗口的终止位置，用两个for循环 完成了一个不断搜索区间的过程。(即一个for循环就是一个滑动窗口,只不过两个
    滑动窗口是严格遍历完数组的所有元素）
    而滑动窗口的精妙之处在于根据当前子序列和大小的情况，不断调节子序列的起始位置。从而将O(n^2)暴力解法降为O(n)。
    那么既然是一个for循环，这个for循环代表的就是滑动窗口的右窗口，当右窗口遍历到数组末端时，循环结束.
    那么我们如何移动窗口左侧呢，我们需要while循环
